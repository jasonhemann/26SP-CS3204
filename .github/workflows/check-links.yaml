name: link-check
on:
  page_build:
  workflow_dispatch:

jobs:
  linkinator:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check published links
        uses: JustinBeckwith/linkinator-action@v2
        with:
          paths: https://hemann.pl/26SP-CS3204
          config: linkinator.config.json
